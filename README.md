# 🎮 Brawl Stars Stats Telegram Bot

Telegram-бот, который получает статистику игрока Brawl Stars по тегу, генерирует красивую карточку и отправляет её в чат и канал.

![Python 3.12](https://img.shields.io/badge/python-3.12-blue)
![aiogram 3](https://img.shields.io/badge/aiogram-3.x-green)

## Возможности

- Получение статистики игрока по тегу (`#XXXXXXXX`)
- Генерация изображения-карточки: имя, трофеи, победы, топ-5 бравлеров
- Автоматическая отправка карточки в Telegram-канал
- Два режима работы: polling (локально) и webhook (Railway / продакшен)

## Быстрый старт (локально)

### 1. Клонируйте репозиторий

```bash
git clone <your-repo-url>
cd brawlstars-bot
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate   # Linux / macOS
venv\Scripts\activate      # Windows
pip install -r requirements.txt
```

### 3. Получите токены

| Токен | Где взять |
|---|---|
| `TELEGRAM_TOKEN` | [@BotFather](https://t.me/BotFather) → `/newbot` |
| `BRAWL_STARS_API_KEY` | [developer.brawlstars.com](https://developer.brawlstars.com) → Create API Key (укажите свой IP) |
| `CHANNEL_ID` | Username канала (`@mychannel`) или числовой ID |

### 4. Настройте `.env`

```bash
cp .env.example .env
# Заполните значения в .env
```

### 5. Запустите

```bash
python bot.py
```

Бот запустится в режиме polling (без webhook).

## Деплой на Railway

### 1. Залейте код в GitHub

```bash
git init && git add . && git commit -m "init"
git remote add origin <your-repo>
git push -u origin main
```

### 2. Создайте проект на [Railway](https://railway.app)

- New Project → Deploy from GitHub Repo
- Выберите репозиторий

### 3. Добавьте переменные окружения

В Railway Dashboard → Variables:

```
TELEGRAM_TOKEN=...
BRAWL_STARS_API_KEY=...
CHANNEL_ID=@your_channel
WEBHOOK_URL=https://<your-app>.up.railway.app
```

> **Важно:** `WEBHOOK_URL` — это публичный домен Railway.  
> Его можно получить: Settings → Networking → Generate Domain.

> **Важно:** API-ключ Brawl Stars привязан к IP. На Railway IP динамический,
> поэтому при создании ключа укажите IP Railway-сервера или используйте
> прокси-сервис. Как вариант — создайте ключ с IP `0.0.0.0` (если API позволяет).

### 4. Deploy

Railway автоматически соберёт Docker-образ и запустит бота.

## Структура проекта

```
brawlstars-bot/
├── bot.py              # Основной код бота
├── requirements.txt    # Зависимости
├── Dockerfile          # Docker-образ
├── Procfile            # Для Railway (альтернатива)
├── railway.toml        # Конфигурация Railway
├── .env.example        # Шаблон переменных
├── .gitignore
└── README.md
```

## Пример карточки

Карточка содержит:
- Имя игрока, тег, клуб
- Уровень (badge)
- Текущие и рекордные трофеи с прогресс-баром
- Статистику побед (3v3, соло, дуо)
- Топ-5 бравлеров по трофеям

## Лицензия

MIT — используйте свободно.
